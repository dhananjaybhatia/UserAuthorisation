<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div
      class="w-full h-screen bg-zinc-900 flex items-center justify-center p-4"
    >
      <div
        class="bg-zinc-800 p-8 rounded-lg shadow-lg text-center max-w-md w-full"
      >
        <h1 class="text-3xl font-bold text-white mb-6">
          Upload Profile Picture`
        </h1>
        <form
          action="/upload"
          method="post"
          enctype="multipart/form-data"
          class="space-y-6"
        >
          <!-- if you don't put this, then multer won't work -->
          <div class="file-input">
            <input
              class="block w-full text-white bg-transparent border-2 border-dashed border-gray-600 rounded-lg p-4 outline-none hover:border-blue-500 focus:border-blue-500"
              type="file"
              name="image"
            />
          </div>
          <button
            class="p-3 bg-blue-500 hover:bg-blue-600 text-white rounded-lg w-full transition duration-300 ease-in-out"
          >
            Upload
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
